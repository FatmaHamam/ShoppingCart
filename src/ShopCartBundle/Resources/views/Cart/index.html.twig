{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cart list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Totalprice</th>
                <th>Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) }}"></a>{{entity.id}}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.totalprice }}</td>
                <td>{{ entity.type }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path("shop_cart_cart_show", {"id": entity.id}) }}">Show</a>
                    </li>
                    <li>
                        <a href="{{ path("shop_cart_cart_edit", {"id": entity.id}) }}">Edit</a>
                    </li>
                    <li>
                        <a href="{{ path("shop_cart_cart_delete", {"id": entity.id}) }}">Delete</a>
                    </li>
                    <li>
                        <a href="{{ path("shop_cart_cart_EmptyCart", {"id": entity.id}) }}">Empty Cart</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) }}">
                Create a new entry
            </a>
        </li>
        
    </ul>
    {% endblock %}
