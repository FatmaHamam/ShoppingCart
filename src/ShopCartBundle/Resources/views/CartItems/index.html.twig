{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CartItems list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cart</th>
                <th>Items</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cartitems_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cart }}</td>
                <td>{{ entity.items }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cartitems_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cartitems_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cartitems_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
